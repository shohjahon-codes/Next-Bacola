import { Category } from "@/types";
import Image from "next/image";


// export const ProductCategory = ({ category }: { category: Category }) => {
//     return (
//         <div>
//             {category.children.map((child: Category) => (
//                 <div key={child.id}>
//                     <Image src={child.image || ''} alt={child.name} width={100} height={100} style={{ width: '200px', height: '200px', objectFit: 'cover' }} />
//                     <div>{child.name}</div>
//                 </div>
//             ))}
//         </div>
//     )
// }
